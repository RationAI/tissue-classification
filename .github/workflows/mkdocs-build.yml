name: MkDocs Build (RationAI Standard)
on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]
  workflow_dispatch:

jobs:
  run:
    uses: RationAI/.github/.github/workflows/mkdocs-build.yml@main
