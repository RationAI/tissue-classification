# @package _global_

defaults:
  - /data: dataset
  - override /class_mapping: collapse_alterations_to_other
  - _self_

embeddings_run_id: ${dataset.mlflow_artifacts.embeddings_run_id}
kfold_run_id: ${dataset.mlflow_artifacts.kfold_run_id}
embed_dim: 2560
n_folds: 5
